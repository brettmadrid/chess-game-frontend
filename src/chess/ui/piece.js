import React from 'react'
import { image } from 'react-konva'

const Piece = props => {
  /**
   * imageURL for this piece
   * color
   * id
   * - color of player
   * - whether or not it's your turn
   * - if is this player's turn, color of this player is the same color as this piece,
   *   then make the piece draggable/moveable
   */
  return <Image drabbable />
}

export default Piece
